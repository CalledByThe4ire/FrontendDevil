range-field-runnable-track($height=1rem, $background=#e1e1e1, $border-color=#000, $border-width=0, $border-radius=0)
	width 100%
	height $height
	background $background
	border $border-width solid $border-color
	border-radius $border-radius
	cursor pointer
